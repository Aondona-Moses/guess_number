import random

# TODO: Write a program that generates a random number between 1 and 100.
# The user should guess the number, and the program should provide feedback:
# "Too high", "Too low", or "Correct!" until the user guesses it correctly.
# HINT: Use random.randint() to generate the number and a while loop for the guesses.

# Generate a random number
number_to_guess = random.randint(1, 100)

# Start your code below:
